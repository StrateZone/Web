export type User = {
  avatar: string;
  name: string;
  skillLevel: string;
  availability: string;
  preferredTime: string;
  chessTypes: string[];
  location: string;
  rating: number;
  matchesPlayed: number;
};
